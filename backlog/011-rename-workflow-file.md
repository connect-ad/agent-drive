# 011 · Rename `Worlflow.md`

**Status:** Open — trivial

The root handoff diagram is spelled `Worlflow.md`. Should be `Workflow.md`.

Left alone so far because nothing depends on the name and renaming it mid-build
would have churned the index for no benefit. Rename it and update the reference
in `CLAUDE.md`.
